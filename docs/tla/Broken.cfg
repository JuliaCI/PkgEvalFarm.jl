\* The protocol as first shipped: an existing pending/running item is
\* "already handled" — nobody ever re-sends its message.
\* Expected: HoldsRelease is VIOLATED (the zombie-derivation trace).
CONSTANTS
    Requesters = {r1, r2}
    MaxMsgs = 2
    Healing = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK ReleasedJustified PublishedMeansDone
PROPERTIES HoldsRelease
