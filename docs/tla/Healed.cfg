\* The fixed protocol: holders (and fresh wants) re-arm derivations that
\* show no sign of life, lease-elected via the enqueued_at CAS.
\* Expected: no violations.
CONSTANTS
    Requesters = {r1, r2}
    MaxMsgs = 2
    Healing = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK ReleasedJustified PublishedMeansDone
PROPERTIES HoldsRelease
